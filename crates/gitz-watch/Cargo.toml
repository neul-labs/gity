[package]
name = "gitz-watch"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1"
notify = { version = "6.1", default-features = true }
thiserror = "1.0"
tokio = { version = "1.37", features = ["sync", "rt", "time"] }

[dev-dependencies]
tempfile = "3.12"
