[package]
name = "gitz-storage"
version = "0.1.0"
edition = "2024"

[dependencies]
gitz-ipc = { path = "../gitz-ipc" }
thiserror = "1.0"
sled = "0.34"
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"

[dev-dependencies]
tempfile = "3.12"
